{
  "compilerOptions": {
    "target": "ES5",
    // 模块解析策略
    "moduleResolution": "node",
    // 指定jsx代码用于的开发环境
    "jsx": "react",
    // 为所有导入模块创建命名空间对象
    "esModuleInterop": true,
    // 用于转换为旧版本的 JS 提供可迭代对象的全面支持
    "downlevelIteration": true,
    // 生成source map文件
    "sourceMap": true,
    // 指定非相对模块名称的基本路径
    "baseUrl": "./",
    // 路径映射,设置模块名称基于baseUrl的路径映射关系
    "paths": {},
    // 允许从没有设置默认导出的模块中默认导入。
    "allowSyntheticDefaultImports": true,
    // 跳过对声明类型的检查
    "skipLibCheck": true,
    // 自动生成声明文件
    "declaration": false,
    // 严格null类型检查
    "strictNullChecks": true,
    // 指定源文件中的根文件夹
    "rootDir": "./src"
  },
  // 排除特定文件
  "exclude": ["node_modules", "lib", "es", "dist", "example"]
}
